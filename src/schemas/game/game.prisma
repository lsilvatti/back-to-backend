model Game { 
    id String @id @default(uuid()) @db.Uuid
    title String
    year Int
    platform String
    genre String[]
    status String

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("games")
}